# API latency

Revisar p50/p95/p99 por ruta, spans HTTP/PostgreSQL, pool y proveedor externo. Distinguir base, routing/geocoding y CPU. Reducir carga o degradar funciones no críticas antes de ampliar timeouts. Confirmar recuperación durante al menos 15 minutos.
