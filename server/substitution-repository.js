import crypto from "node:crypto";
import {postgresPool} from "./postgres.js";
import {enqueueNotificationForInternalUser} from "./notification-repository.js";

const newId=()=>`SUB-${crypto.randomBytes(6).toString("hex").toUpperCase()}`;
const money=value=>Number(value||0)/100;
const map=row=>({id:row.public_id,orderId:row.job_public_id,status:row.status,quantity:row.quantity,reason:row.reason,original:{id:row.original_public_id||row.original_snapshot.publicId,name:row.original_snapshot.name,unitPrice:money(row.original_unit_price_cents)},replacement:{id:row.replacement_public_id||row.replacement_snapshot.publicId,name:row.replacement_snapshot.name,unitPrice:money(row.replacement_unit_price_cents)},refundAmount:money(row.refund_amount_cents),createdAt:new Date(row.created_at).toISOString(),decidedAt:row.decided_at?new Date(row.decided_at).toISOString():null});

const select=`SELECT s.*,j.public_id job_public_id,j.customer_id,original.public_id original_public_id,replacement.public_id replacement_public_id FROM order_item_substitutions s JOIN jobs j ON j.id=s.job_id JOIN catalog_items original ON original.id=s.original_catalog_item_id JOIN catalog_items replacement ON replacement.id=s.replacement_catalog_item_id`;

export async function proposeOrderSubstitution({orderPublicId,merchantOwnerPublicId,originalMenuItemId,replacementMenuItemId,reason,admin=false}){
  const client=await postgresPool.connect();try{await client.query("BEGIN");
    const row=(await client.query(`SELECT j.id job_id,j.customer_id,ji.id job_item_id,ji.quantity,ji.unit_price_cents original_price,ji.name original_name,original.id original_id,original.public_id original_public_id,original.available original_available,replacement.id replacement_id,replacement.public_id replacement_public_id,replacement.name replacement_name,replacement.unit_price_cents replacement_price
      FROM jobs j JOIN merchants m ON m.id=j.merchant_id JOIN users owner ON owner.id=m.owner_id JOIN job_items ji ON ji.job_id=j.id JOIN catalog_items original ON original.id=ji.catalog_item_id JOIN catalog_items replacement ON replacement.merchant_id=m.id AND replacement.public_id=$4
      WHERE j.public_id=$1 AND j.kind='delivery' AND j.metadata->>'subtype'='food_order' AND j.status IN('accepted','preparing') AND ($5::boolean OR owner.public_id=$2) AND original.public_id=$3 FOR UPDATE OF j,ji`,[orderPublicId,merchantOwnerPublicId,originalMenuItemId,replacementMenuItemId,admin])).rows[0];
    if(!row)throw Object.assign(new Error("Pedido, producto o comercio no disponible para sustituir"),{status:404});
    if(row.original_available)throw Object.assign(new Error("Marca el producto original sin stock antes de sustituirlo"),{status:409});
    if(row.original_id===row.replacement_id||Number(row.replacement_price)>Number(row.original_price))throw Object.assign(new Error("El reemplazo debe ser distinto y de precio igual o menor"),{status:409});
    const inserted=(await client.query(`INSERT INTO order_item_substitutions(public_id,job_id,job_item_id,original_catalog_item_id,replacement_catalog_item_id,proposed_by,quantity,original_unit_price_cents,replacement_unit_price_cents,reason,original_snapshot,replacement_snapshot)
      SELECT $1,$2,$3,$4,$5,u.id,$6,$7,$8,$9,$10,$11 FROM users u WHERE u.public_id=$12 RETURNING *`,[newId(),row.job_id,row.job_item_id,row.original_id,row.replacement_id,row.quantity,row.original_price,row.replacement_price,reason,{publicId:row.original_public_id,name:row.original_name},{publicId:row.replacement_public_id,name:row.replacement_name},merchantOwnerPublicId])).rows[0];
    inserted.job_public_id=orderPublicId;inserted.original_public_id=row.original_public_id;inserted.replacement_public_id=row.replacement_public_id;
    await enqueueNotificationForInternalUser(client,{userId:row.customer_id,template:"order_substitution",payload:{jobId:orderPublicId,substitutionId:inserted.public_id,status:"pending",original:row.original_name,replacement:row.replacement_name},deduplicationKey:`substitution:${inserted.public_id}:pending`});
    await client.query("COMMIT");return map(inserted);
  }catch(error){await client.query("ROLLBACK");if(error.code==="23505")throw Object.assign(new Error("Ya existe una sustitución pendiente para este producto"),{status:409});throw error;}finally{client.release();}
}

export async function getOrderSubstitutions({orderPublicId,actorPublicId,roles}){const admin=roles.includes("admin"),merchant=roles.includes("merchant");const result=await postgresPool.query(`${select} JOIN merchants m ON m.id=j.merchant_id JOIN users owner ON owner.id=m.owner_id WHERE j.public_id=$1 AND ($3::boolean OR j.customer_id=(SELECT id FROM users WHERE public_id=$2) OR ($4::boolean AND owner.public_id=$2)) ORDER BY s.created_at DESC`,[orderPublicId,actorPublicId,admin,merchant]);return result.rows.map(map);}

export async function decideOrderSubstitution({substitutionPublicId,customerPublicId,decision}){
  const client=await postgresPool.connect();try{await client.query("BEGIN");
    const row=(await client.query(`${select} WHERE s.public_id=$1 AND s.status='pending' AND j.customer_id=(SELECT id FROM users WHERE public_id=$2) AND j.status IN('accepted','preparing') FOR UPDATE OF s,j`,[substitutionPublicId,customerPublicId])).rows[0];
    if(!row)throw Object.assign(new Error("Sustitución no encontrada, ya decidida o fuera de etapa"),{status:409});const customer=(await client.query("SELECT id FROM users WHERE public_id=$1",[customerPublicId])).rows[0];let refundCents=0;
    if(decision==="accepted"){
      refundCents=(Number(row.original_unit_price_cents)-Number(row.replacement_unit_price_cents))*Number(row.quantity);
      await client.query(`UPDATE job_items SET catalog_item_id=$2,name=$3,unit_price_cents=$4,metadata=metadata||$5::jsonb WHERE id=$1`,[row.job_item_id,row.replacement_catalog_item_id,row.replacement_snapshot.name,row.replacement_unit_price_cents,JSON.stringify({publicId:row.replacement_public_id,substitutionId:row.public_id,original:row.original_snapshot})]);
      await client.query(`UPDATE jobs SET quoted_amount_cents=quoted_amount_cents-$2,final_amount_cents=final_amount_cents-$2,metadata=jsonb_set(jsonb_set(metadata,'{subtotal}',to_jsonb((COALESCE((metadata->>'subtotal')::numeric,0)-$2/100.0))),'{discount}',to_jsonb(LEAST(COALESCE((metadata->>'discount')::numeric,0),COALESCE((metadata->>'subtotal')::numeric,0)-$2/100.0))),version=version+1,updated_at=now() WHERE id=$1`,[row.job_id,refundCents]);
      if(refundCents){const payment=(await client.query("SELECT * FROM payment_intents WHERE job_id=$1 AND provider='flash_wallet' AND status IN('captured','partially_refunded') FOR UPDATE",[row.job_id])).rows[0];if(!payment||Number(payment.captured_amount_cents)<refundCents)throw Object.assign(new Error("No existe captura suficiente para ajustar el pedido"),{status:409});const wallet=(await client.query("SELECT id FROM ledger_accounts WHERE owner_type='user' AND owner_id=$1 AND account_type='wallet' FOR UPDATE",[row.customer_id])).rows[0],clearing=(await client.query("SELECT id FROM ledger_accounts WHERE owner_type='platform' AND owner_id IS NULL AND account_type='cash_clearing' FOR UPDATE")).rows[0],transaction=(await client.query(`INSERT INTO ledger_transactions(idempotency_key,kind,actor_id,description,metadata) VALUES($1,'refund',$2,$3,$4) RETURNING id`,[`substitution-refund-${row.public_id}`,customer.id,`Diferencia sustitución ${row.job_public_id}`,{substitutionId:row.public_id}])).rows[0];await client.query(`INSERT INTO ledger_entries(transaction_id,account_id,direction,amount_cents,reference_type,reference_id,metadata) VALUES($1,$2,'credit',$3,'substitution',$4,$5),($1,$6,'debit',$3,'substitution',$4,$5)`,[transaction.id,wallet.id,refundCents,row.job_id,{substitutionId:row.public_id},clearing.id]);await client.query(`INSERT INTO refunds(payment_intent_id,requested_by,amount_cents,reason,status,resolved_at,provider_refund_id) VALUES($1,$2,$3,'substitution','succeeded',now(),$4)`,[payment.id,customer.id,refundCents,row.public_id]);const remaining=Number(payment.captured_amount_cents)-refundCents;await client.query("UPDATE payment_intents SET captured_amount_cents=$2,status=$3,updated_at=now() WHERE id=$1",[payment.id,remaining,remaining?"partially_refunded":"refunded"]);}
    }
    const updated=(await client.query("UPDATE order_item_substitutions SET status=$2,decided_by=$3,decided_at=now(),refund_amount_cents=$4 WHERE id=$1 RETURNING *",[row.id,decision,customer.id,refundCents])).rows[0];Object.assign(updated,{job_public_id:row.job_public_id,original_public_id:row.original_public_id,replacement_public_id:row.replacement_public_id});await client.query("COMMIT");return map(updated);
  }catch(error){await client.query("ROLLBACK");throw error;}finally{client.release();}
}
